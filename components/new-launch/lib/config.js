// Re-export everything from the main project's config
// so form submissions go to the same API
export { PROJECT_ID, PROJECT_NAME, CITY_DISPLAY, CITY_SLUG, CITY_ID, SHEET_NAME, SECRET_KEY, API_ENDPOINT, SHEET_WEBHOOK, PROPTIGER_URL } from '../../../lib/config'
